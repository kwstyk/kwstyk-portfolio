---
// propsを受け取る
const { title, link, class: className } = Astro.props;
---

<a href={link} class={`block p-4 rounded-lg shadow-md bg-card-bg hover:bg-card-accent transition ${className}`}>
  <h3 class="text-lg font-bold text-text mb-2">{title}</h3>
  <slot />
</a>
