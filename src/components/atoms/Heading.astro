---
interface Props {
  level?: 1 | 2 | 3 | 4;
  className?: string;
}

const { level = 1, className = '' } = Astro.props as Props;

const Tag = `h${level}` as keyof HTMLElementTagNameMap;
---

<Tag class={`heading h${level} ${className}`}>
  <slot />
</Tag>
