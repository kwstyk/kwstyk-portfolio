---
import Card from '@/components/molecules/Card.astro'; // ← これを必ず書く！

const { title, description, link, className } = Astro.props;
---

<Card title={title} description={description} href={link} class={className}>
  <p class="text-text-muted">{description}</p>
</Card>
